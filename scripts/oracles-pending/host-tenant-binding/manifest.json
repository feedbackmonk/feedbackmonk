{
  "name": "host-tenant-binding",
  "version": "1.0.0",
  "kind": "verification",
  "question": "When a public request arrives on a host that resolves to tenant T, can any public route return or act on another tenant's resource -- and is the admin surface reachable on any host other than the canonical admin host? Probe A proves from `build_app` that every public router is wrapped in `bind_public_routes` and every admin router in `bind_admin_routes` (the anti-treadmill leg: a NEW public route that forgets the guard fails here, exactly as public-route-ceiling guards the rate-limit floor). Probe B proves from `hosting.rs` that `admin_host_binding` maps HostScope::Tenant straight to 404, redirects HostScope::AdminAlias to the canonical admin host (DEC-FBR-IMPL-27), never returns 403 (which would be an existence oracle), and gates X-Forwarded-Host on the declared trusted proxy. Probe C proves there is exactly ONE host->tenant resolution path, that it lives in the repository crate (DEC-FBR-03), and that no other module reads the Host header ad hoc. Probe D (--full) runs the behavioural fixtures.",
  "category": "security",
  "invocation": {
    "unix": ".claude/oracles/host-tenant-binding/oracle.sh",
    "windows": ".claude/oracles/host-tenant-binding/oracle.ps1"
  },
  "output": {
    "format": "text",
    "schema": "PASS or FAIL <n> with probe-level offender detail. Exit 0 on PASS, 1 on FAIL, 2 on environment failure. Vacuous-PASS when the host layer is absent from the tree."
  },
  "freshness": {
    "strategy": "trigger-invalidate",
    "triggers": [
      "crates/feedbackmonk-api/src/main.rs",
      "crates/feedbackmonk-api/src/hosting.rs",
      "crates/feedbackmonk-api/src/handlers/public_site.rs",
      "crates/feedbackmonk-api/src/handlers/domains.rs",
      "crates/feedbackmonk-repository/src/domains.rs",
      "crates/feedbackmonk-core/src/hosting.rs",
      "crates/feedbackmonk-api/tests/host_tenant_binding.rs",
      "crates/feedbackmonk-repository/tests/domains_repo.rs",
      ".claude/oracles/host-tenant-binding/oracle.py"
    ],
    "compute_cost_ms": 60
  },
  "consultation": {
    "typical_sessions_using": "every commit that adds or rewires a public route, touches build_app, or changes the host layer",
    "estimated_token_savings_per_call": 1100
  },
  "consumer_scope": "feedbackmonk: anyone adding a public or admin route, and every reviewer of the hosting shape (FR-FBR-32/33). Green is the FR-FBR-32 exit gate.",
  "validation": {
    "self_test_unix": null,
    "self_test_windows": null,
    "last_validated": "2026-08-30T00:00:00Z"
  },
  "fallback": "Probe A: open crates/feedbackmonk-api/src/main.rs::build_app and confirm every public router is merged as bind_public_routes(...) and every admin router as bind_admin_routes(...). Probe B: read crates/feedbackmonk-api/src/hosting.rs::admin_host_binding and confirm HostScope::Tenant => not_found() and HostScope::AdminAlias => redirect_to_admin(). Probe C: grep -rn 'resolve_host(' crates/ and confirm only the repository impl, hosting.rs and public_site.rs name it. Probe D: cargo test -p feedbackmonk-api --test host_tenant_binding && cargo test -p feedbackmonk-repository --test domains_repo.",
  "provenance": {
    "authored_by": "DEFER-005 build lane (FR-FBR-32/33). Assertion block frozen at Task Zero, BEFORE any implementation existed, per the injecting brief's explicit instruction.",
    "hardening_note": "Adversarially self-tested during authoring: (1) dropping bind_public_routes from widget_config_router and bind_admin_routes from moderation_router -> Probe A FAIL naming both, exit 1; (2) flipping the cross-tenant refusal from 404 to 403 -> Probe B FAIL. Both restored and re-verified PASS. Probe A is parenthesis-balanced over `.merge(` arguments rather than regex-split, because the real arguments nest (bind_public_routes(apply_public_rate_limit(board_router(state), prl), hs)) and a naive split truncates at the first `)` -- which would have produced a silent false PASS. INTENTIONALLY_UNBOUND records the two deliberate exemptions (health_router, capabilities_router) with reasons, so a reviewer sees the exemptions rather than inferring them from absence.",
    "created": "2026-08-30T00:00:00Z",
    "rationale": "DEC-FBR-13 chose tenant subdomains over path-based multi-tenancy for ONE decisive reason: origin isolation for the user-generated-content surface. That reason is bought only by BINDING -- resolution alone buys nothing, and its absence is invisible at runtime because a missing guard renders a perfectly correct-looking page of another tenant's feedback on your origin. No happy-path test can see it. multi-tenant-isolation-check does not cover it either: that oracle polices the REPOSITORY scope axis, and a router merged without the host guard passes it unchanged. This is detection-from-code over a second, orthogonal isolation axis introduced by FR-FBR-32."
  }
}
