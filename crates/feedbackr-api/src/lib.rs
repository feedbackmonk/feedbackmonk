//! Feedbackr API crate. Stage 1 ships a placeholder; Stage 2 Workers A and B
//! flesh out signup/onboarding (Worker A) and the submission path (Worker B).

#[must_use]
pub fn placeholder() -> &'static str {
    "feedbackr-api stage1 placeholder"
}
