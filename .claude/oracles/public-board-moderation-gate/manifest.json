{
  "name": "public-board-moderation-gate",
  "version": "1.1.0",
  "kind": "verification",
  "question": "Can any public-board endpoint (READ or VOTE) return/act on a feedback row whose moderation_status != approved, or leak submitter PII? Probe A proves from moderation.rs that `is_publicly_visible` classifies EXACTLY {Approved} as visible (and excludes Pending/Rejected). Probe B (ACTIVE) asserts — within the BOARD READ SCOPE (board.rs handler + every repository fn named *board* that queries `FROM feedback`) — that the handler invokes the approved-only board reads (not an unfiltered feedback read), each board read fn hard-filters moderation_status='approved' as a SQL literal (not a bound param), the scope names no submitter-PII field, references no non-approved moderation literal, and does not surface feedback_replies; AND (v1.1.0, vote path D3 / PF-BOARD-VOTING-01) that every board.rs handler writing through state.board_votes (cast/retract) runs ensure_board_enabled + an approved-only resolution (resolve_approved_board_*) BEFORE the write, so a vote/retract on a pending/rejected/board-disabled item 404s like the read path. Probe C (--full, ACTIVE) runs the board moderation-gate + privacy isolation tests.",
  "category": "security",
  "invocation": {
    "unix": ".claude/oracles/public-board-moderation-gate/oracle.sh",
    "windows": ".claude/oracles/public-board-moderation-gate/oracle.ps1"
  },
  "output": {
    "format": "text",
    "schema": "PASS or FAIL <n> with probe-level offender detail. Exit 0 on PASS, 1 on FAIL, 2 on environment failure. Probe B/C report PENDING (not failure) until Worker A's board repo/handler + integration tests land."
  },
  "freshness": {
    "strategy": "trigger-invalidate",
    "triggers": [
      "crates/feedbackmonk-core/src/moderation.rs",
      "crates/feedbackmonk-repository/src/feedback.rs",
      "crates/feedbackmonk-repository/src/board_votes.rs",
      "crates/feedbackmonk-api/src/handlers/board.rs",
      "crates/feedbackmonk-api/src/handlers/moderation.rs",
      "crates/feedbackmonk-api/tests/board_moderation_gate.rs",
      "crates/feedbackmonk-api/tests/board_privacy_isolation.rs",
      "crates/feedbackmonk-api/tests/board_vote_moderation_gate.rs",
      ".claude/oracles/public-board-moderation-gate/oracle.py"
    ],
    "compute_cost_ms": 45
  },
  "consultation": {
    "typical_sessions_using": "every-commit touching the moderation state machine, the board read path, or the board wire shapes",
    "estimated_token_savings_per_call": 950
  },
  "consumer_scope": "feedbackmonk: Worker A (primary) + Workers C/D + every reviewer touching crates/feedbackmonk-core/src/moderation.rs (state machine), the board read repo/handler, or the board wire shapes. A green oracle with Probe B+C ACTIVE is the Stage 1 exit gate (GATE 1).",
  "validation": {
    "self_test_unix": null,
    "self_test_windows": null,
    "last_validated": "2026-06-19T00:00:00Z"
  },
  "fallback": "Probe A: read crates/feedbackmonk-core/src/moderation.rs and confirm `is_publicly_visible` returns true for EXACTLY {Approved} and false for {Pending, Rejected}. Probe B: read the board read repo/handler and confirm the SQL hard-filters moderation_status='approved' (not an optional/handler-side filter) AND the board wire shape names none of {end_user_email, end_user_name, end_user_sub, anon_token_hash, external_metadata, crash_event_id}. Probe C: cargo test -p feedbackmonk-api --test board_moderation_gate --test board_privacy_isolation.",
  "provenance": {
    "authored_by": "Stage 0 freeze (scaffold) — Probe B/C finalized + hardened + ACTIVATED against the board path at Stage 1. Probe A+B+C all green (GATE 1 oracle criterion met). v1.1.0 (PF-BOARD-VOTING-01): Probe B EXTENDED to the vote path (D3) — every board.rs handler writing through state.board_votes must run ensure_board_enabled + an approved-only resolution (resolve_approved_board_*) BEFORE the write; the resolution fn is itself a board-read fn so its approved-only SQL literal is pinned by the per-read-fn check. Gracefully skipped before voting is wired.",
    "hardening_note": "v1.0.0: Probe B is BOARD-READ-SCOPED — the board read fns are repository fns named *board* that query `FROM feedback` (excludes the PII-allowed admin queue `list_pending_for_admin` AND board-SETTINGS fns that query `projects`). Closes the stub's false-PASS hole (it searched all of feedback.rs and could be satisfied by an unrelated query's marker). The approved-only literal is required PER read fn (a regression dropping the filter from just one of the two reads is caught, not masked by the other). PII + feedback_replies scans cover the handler wire shape AND each repo read fn body (a PII column SELECTed in the repo query was previously missed). APPROVED_SQL marker is whitespace/quote/::cast/IN('approved')-tolerant but still LITERAL-only (a bound param can't be statically proven always-'approved' and is correctly rejected). Added: non-approved-literal guard (catches != 'rejected' / IN('approved','pending') leaks); handler-binding check (the handler must invoke the approved-only board reads, mirroring approval-gate-enforcement — catches a handler rewired to an unfiltered feedback read WITHOUT needing --full). Probe B/C activated at SP1: the `_iter_fn_bodies` extractor skips signature-only trait declarations (the real feedback.rs has list_public_board as both a trait decl and an impl — the naive text.find() grabbed the decl and mis-extracted; caught only by running against real code). Validated by an adversarial self-test (14/14: correct path + 4 spelling variants PASS; 9 bypass attempts FAIL, incl. trait-decl/impl and handler-bypass) kept in the worker workspace. Static-probe boundary (documented): C29 inv. 2 (board-disabled→404, approved-rows-from-non-opted-in-project) is outside this oracle's framed question (non-approved OR PII) and is covered by the behavioral Probe C (board_moderation_gate.rs::board_disabled_project_404s).",
    "created": "2026-06-19T00:00:00Z",
    "rationale": "Testability Gate Flag 1 (Q2=5, highest plan-wide fidelity risk): the moderation gate is THE trust boundary between public submission and public exposure (FR-FBR-25a sibling). A happy-path test confirms `approve -> visible` but cannot prove the ABSENCE of a code path that returns an unapproved row on the public board, or that leaks submitter PII. This oracle is detection-from-CODE (state-machine source + SQL filter + wire shape + ledger), the anti-reward-hacking leg — it cannot be satisfied by a self-reported `is_public` flag. Scaffolded in Stage 0 (Probe A live; B/C co-evolve with Worker A); a green oracle with B+C active is GATE 1."
  }
}
