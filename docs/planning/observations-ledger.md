# Observations Ledger

One line per finding that did **not** clear the `/0-uldf-inject` Phase 2.5 filing gate (user
directive 2026-08-06): internally-discovered with no observed harm, unmeasured inference, or a
verification-machinery coverage gap with no witnessed wrong verdict. Append-only; newest last.
A line is promoted to a DEFER brief by whoever actually **observes** the harm it predicts —
cite the line when promoting. This file is deliberately not read at session start and has no
oracle; it is a grep surface, not a work queue.

Format: `YYYY-MM-DD | one-sentence finding | where seen | filed-by`

---
2026-08-06 | ULDF's DISC-SPAWN-02 burst guard is enforced in code only along the path an LD *chooses* (`spawn-pods-all.ps1`); nothing stops an LD from firing N direct `spawn-claude-session.ps1` calls instead, and the only defense on that path is the segment's prose "`--all` MUST go through the orchestrator" — no PreToolUse hook covers spawn *rate* (`pre-tool-use-ctd-spawn-gate` covers designation only). The 2026-06-18 feedbackmonk crash IS consistent with such a bypass (spawns 6s apart while a worker's pid took 19s — timings the orchestrator's verify-between gate could not produce), but attribution is NOT established: `~/.claude/` carries no sync-provenance record, so whether the guard had even reached that machine 9 days after it landed in `claude-template/` is undeterminable | measured while draining feedbackmonk DEFER-001; framework commit `432cc833` (2026-06-09) vs incident 2026-06-18, registry `.claude/collaboration/archived/collab-20260618-180600/` | defer-drain-FeedbackMonk-20260806 (ledger not brief under the 2026-08-06 filing gate: the harm is real but is 7 weeks old and **cannot be attributed** between bypass and sync-lag — filing a brief into ULDF on an unverified attribution would be exactly the unmeasured inference the gate exists to stop. Promote when an LD is witnessed bursting direct spawns with the orchestrator demonstrably present)
2026-08-21 | This project's live Pending Follow-Ups use `### PF-XXX:` headings, but the `pending-followups` oracle only matches the bullet form `^-\s+\*\*...\*\*` (run.sh L127), so PF-PHASEA-01 and PF-DEPLOY-01 are invisible to it — the oracle reports neither, and its briefing line cannot name them. No harm witnessed: CLAUDE.md is loaded into every session's prompt in full, which is the documented primary firing mechanism (`/0-uldf-schedule` § Purpose), so the entries do still reach every session; only the machine-readable surfacing is missing. Measured: `grep -n '^### PF-' CLAUDE.md` -> 3 live entries; `bash .claude/oracles/pending-followups/run.sh | grep -i <title>` -> no match for the two above. PF-UNPIN-01 was given a bullet-form index line so it surfaces (verified) | seen while adding PF-UNPIN-01 during the DEC-405 oracle refresh; commit 08243b4 | oracle-refresh-FeedbackMonk (ledger not brief under the 2026-08-06 filing gate: internally discovered, no observed harm, and the fix is a project-side format choice, not an oracle defect — the oracle parses exactly the form /0-uldf-schedule mandates. Promote if a dated PF entry is ever missed *because* the oracle could not see it)
2026-08-30 | The public feedback board is a route inside the ADMIN SPA, not a separate app: `admin-ui/src/App.tsx:35,65` maps `/public/projects/:projectId/board` to `PublicBoard` in the same bundle that serves `Login.tsx`, triage, moderation and settings. Today that is harmless -- the only deployment is GitCellar's single-tenant self-host at `feedback.gitcellar.com`, where there is no second tenant to cross into and the admin is a separate host (`triage.gitcellar.com`). It becomes load-bearing only IF the SaaS ships on the path-based shape the P3 gate committed to (`feedbackmonk.com/{tenant}/{project}/roadmap`, plan L252): then every tenant's board -- rendering user-submitted text, the product's core content -- shares one origin with every other tenant's board and with the admin console, and same-origin policy is per-origin, not per-path. Not measured against a running multi-tenant instance because none exists | seen while answering a GitCellar-side question about feedbackmonk hosting shape; routed to OPEN_QUESTIONS Q21 as the deciding evidence | hosting-shape-question-20260830 (ledger not brief under the 2026-08-06 filing gate: internally discovered, no observed harm, and the condition that would create harm -- a path-based multi-tenant deployment -- is an UNMADE owner decision, not a defect. Promote if the path-based shape is chosen and a second tenant lands on it)
2026-08-30 | RESOLVES the 2026-08-30 shared-origin line above (`hosting-shape-question-20260830`): its premise was re-measured and still holds — `grep -n "PublicBoard\|path=" admin-ui/src/App.tsx` shows the public board is STILL a route inside the admin SPA (App.tsx:60-67, same bundle as Login.tsx) — but the harm it predicted was conditional on the path-based shape, and DEC-FBR-13 went the other way. Under FR-FBR-32 each tenant's board lives on its own ORIGIN (`{tenant}.feedbackmonk.com` or their custom domain) and the admin console on a third (`app.feedbackmonk.com`), which is the whole of the origin-isolation the line was worried about; same-origin policy is per-origin, and host separation delivers that regardless of which JS bundle is served. So the line does NOT promote to a brief. What remains is bundle hygiene, not isolation: the admin bundle's bytes are still served on tenant origins, which costs payload and attack surface but cannot reach the admin origin's cookies. Judged in-lane and deliberately NOT put on the FR-FBR-32 critical path; recorded as a follow-on in `docs/operations/SAAS_HOSTING.md` § Deferred. Promote only if a path-based multi-tenant deployment is ever chosen after all, or if the board bundle is found to leak something admin-only at build time | measured while implementing DEC-FBR-13/14 (DEFER-005); binding proven by `crates/feedbackmonk-api/tests/host_tenant_binding.rs` | defer-005-build-lane-FeedbackMonk (ledger not brief: this closes a conditional line whose condition did not occur, and the residual item is a hygiene preference with no witnessed harm)
2026-09-06 | scripts/i18n/gen-locales.sh's python-not-found branch prints `FAIL host-tenant-binding (python3 not found)` -- a copy-paste leftover from the oracle shim template it was cloned from; the exit code (2) is still correct, only the message names the wrong tool. No observed harm (this branch only fires when no python3/python/py is on PATH, which has not happened in this session) | seen authoring scripts/i18n/{check-gaps,translate,validate,complete-plurals}.sh shims for FR-FBR-39 and comparing against the existing gen-locales.sh template; gen-locales.py is LEAD-frozen so not fixed inline | CLAUDE-D-collab-20260906-215851 (ledger not brief: cosmetic-only, no observed harm, file is LEAD-owned/frozen not mine to edit this session. Promote if a real python-not-found failure is ever hit and the wrong tool name in the message costs someone time diagnosing it) | **RESOLVED 2026-09-07 by the LD at Stage-1 converge** — message corrected in the same tree (`FAIL gen-locales (python3 not found)`); line kept as the record.
2026-09-07 | `widget-bundle-size` was amended to 1.1.0 in this commit -- Probe A redefined to the English page-load set and a NEW Probe C added (per-locale-chunk cap 4096 B) -- but its manifest declares `self_test_unix: null` / `self_test_windows: null`, so `--self-test` is silently ignored and falls through to an ordinary run. Its three new i18n siblings (`i18n-catalog-integrity`, `i18n-literal-ratchet`, `translation-gap-status`) each carry a real invertible self-test that was run and passed at this finalize. Effect: the probe that now defends the 30-chunk budget has never been demonstrated able to go RED, so a Probe C that silently stopped measuring would present as a clean PASS. No wrong verdict witnessed -- Probe C actively passed here at 30 chunks / largest 53 B / 4043 B headroom, and the cap constant `SIZE_CAP_BYTES` was not touched | measured at /0-uldf-finalize Phase 11 revalidation of the i18n Stage-1 commit: `bash .claude/oracles/widget-bundle-size/oracle.sh --self-test` runs the normal oracle and prints no self-test lines, vs. 3/5/2 self-test assertions respectively in the three siblings | finalize-i18n-stage1-20260907 (ledger not brief under the 2026-08-06 filing gate: a coverage gap in verification machinery with no witnessed wrong verdict. Promote if Probe C is ever found green while a chunk actually exceeds the cap, or when the oracle is next edited -- author the self-test then). **Probe C's falsifiability WAS demonstrated at this same finalize** -- padding one real chunk 53 B -> 4286 B turned the oracle RED with a named offender and exit 1, restored byte-identically (sha invariant verified); receipt at `docs/falsifiability/2026-09-07-widget-bundle-size-probe-c.md`. A one-off receipt is not a self-test, though: it demonstrates the probe at THIS commit and cannot catch a later edit that quietly defeats it, so the line stands
2026-09-07 | This project's `.gitignore` is missing 5 framework hygiene patterns the `gitignore-template-drift` oracle names: `**/.claude/collaboration/collab-*/`, `**/.claude/finalize-state/`, `**/oracles/*/cache/`, `**/.claude/handoff/handoff-*.md`, `**/.claude/collaboration/_coordination-gc.jsonl`. The `finalize-state` one is no longer hypothetical -- this finalize run's Phase 11 §5b critic-audit watermark landed at `.claude/finalize-state/critic-audit-watermark.json` as an UNTRACKED file, i.e. the framework's own per-machine state was offered to the commit. That single pattern was added here so the run does not strand its own artifact; the other four are left to `/0-uldf-migrate-hygiene`, which is the command that owns this migration. Also noted while there: the framework-canonical finalize run-summary path `.claude/runs/<run-id>/` is covered by no baseline pattern at all, so this run wrote its summary under the gitignored `.claude/session-state/runs/` instead | measured at /0-uldf-finalize Phase 11: `bash .claude/oracles/gitignore-template-drift/run.sh` -> `drifted: true, missing_patterns: [5]`; `git check-ignore -v .claude/finalize-state/critic-audit-watermark.json` -> no match | finalize-i18n-stage1-20260907 (ledger not brief under the 2026-08-06 filing gate: internally discovered, no observed harm -- nothing was wrongly committed -- and the remaining four have a named one-command route. Promote if per-machine state is ever actually committed because a pattern was missing)
2026-09-07 | The C35 plural table is implemented THREE times and two of them disagree: `crates/feedbackmonk-i18n/src/catalogs.rs::plural_category` (the renderer) can select a category that `scripts/i18n/_catalog.py::plural_categories` (the authoring partition, which decides which `key_<cat>` suffixes translators are ever asked to fill) never writes. For **ja, ko, zh-CN, zh-HK, zh-TW, id, tr, fa** the catalog carries only `_other`, but the renderer returns `One` at n=1, so `t_plural` misses the target catalog and `lookup`'s per-key fallback hands back ENGLISH -- a translated email would render an English sentence at singular counts and the target language at plural ones. Splits two ways against CLDR: for ja/ko/zh-*/id the Python side is right and the Rust generic `n==1 -> one` arm is wrong; for tr/fa the Rust side is right and `OTHER_ONLY` wrongly includes them. Separately the Rust table also disagrees with CLDR for **ga** (Irish genuinely has zero/one/two/few/many/other; Rust models none of it), **is** (n%10==1 && n%100!=11), and **fr / pt-BR / si / fa** at n=0. NO OBSERVED HARM: the `en` email/status catalogs currently define zero plural keys, so `t_plural` has no live call site -- this is latent until the first one is added | measured at /0-uldf-finalize Phase 5 categorization of the i18n Stage-1 commit, by differencing both partitions against Node's ICU-backed `Intl.PluralRules` over counts 0..=1000; all three runtimes are now pinned to the new shared fixture `i18n/plural-fixtures.json`, whose two ratchet ledgers hold the divergence sets EXACTLY (a ninth locale, or a heal, turns the suite red) -- `crates/feedbackmonk-i18n/tests/plural_partition.rs`, `scripts/i18n/tests/test_plural_partition.py`, `widget/src/plural-partition.test.ts` | finalize-i18n-stage1-phase5-20260907 (ledger not brief under the 2026-08-06 filing gate: internally discovered, no observed harm, and which of the two partitions to move is a product i18n-correctness call -- notably whether Turkish and Persian get a `_one` form -- not a defect with one obvious fix. The divergence can no longer widen silently, so the cost of deciding later is bounded. Promote the moment a plural key is added to `i18n/locales/en/{email,status}.json`, which makes it user-visible)
2026-09-07 | PARTIALLY RESOLVES the C35 three-way plural-partition line above: the **tr/fa half is fixed** -- `scripts/i18n/_catalog.py`'s `OTHER_ONLY` no longer lists Persian or Turkish (CLDR gives each `{one, other}`; re-measured here with Node's ICU `Intl.PluralRules` over counts 0..=1000: `fa` selects `one` at n=0 and n=1, `tr` at n=1), so `ledger.english_fallback_locales` in `i18n/plural-fixtures.json` shrank from 8 codes to 6 and `validate.py`'s `plural_category_gap` class now requires a `_one` form in the fa/tr catalogs (demonstrated in a temp mirror: a Persian `public.roadmap.voteCount_other` with no `_one` is now an error, and was silently accepted before). What stays OPEN and out of scope (LD ruling R-4, collab-20260907-034037): the **Rust-side** half -- `catalogs.rs::plural_category`'s generic `n==1 -> one` arm still selects `one` for ja/ko/zh-CN/zh-HK/zh-TW/id whose catalogs correctly carry only `_other` (the remaining 6 English-fallback codes), and it still disagrees with ICU at n=0 for fr/pt-BR/fa/si, for ga (Irish's real zero/one/two/few/many) and for is (n%10==1 && n%100!=11) -- all 12 still pinned in `ledger.icu_partition_mismatch_locales`. NO OBSERVED HARM for the remainder: `i18n/locales/en/{email,status}.json` still define zero plural keys, so the Rust `t_plural` has no live call site | measured 2026-09-07 by CLAUDE-F of PODS collab-20260907-034037 (Stage 2 of the UI-localization arc) while executing the assigned `OTHER_ONLY` correction; three plural-partition suites re-run red -> green -> inverse-red | ledger not brief under the 2026-08-06 filing gate: the remainder is internally discovered, still latent, and R-4 scoped it out of this wave -- promote the moment a plural key is added to `i18n/locales/en/{email,status}.json`, which makes the Rust half user-visible
2026-09-07 | Of the nine `ApiError` codes added by FR-FBR-40, SIX now have a `widget.error.<code>` key (`invalid_input`, `unauthorized`, `not_found`, `payload_too_large`, `tier_cap`, `internal`) and THREE do not (`forbidden`, `conflict`, `gone`), so `widget/src/ui.ts::errorKey` falls through to `widget.error.generic` for those three. The three unkeyed codes were each verified NOT widget-reachable, by construction site: `forbidden` only at `auth/session.rs:102`, `handlers/login.rs:125` and `handlers/work_orders.rs:191,373` (admin session / login / work-order surfaces); `gone` only at `handlers/verify_email.rs:89` (account verification); `conflict` only in `admin_feedback.rs` + `clusters.rs` (admin), plus `From<RepoError>`, whose sole submit-path source is `feedback.rs::claim_idempotency_key` — reachable only with an `Idempotency-Key` header the widget never sends. The widget calls exactly three routes (`api.ts:54` widget-config, `api.ts:75` submit, `attachments.ts:367` upload). `internal` IS on that reachable set and WAS a real regression, now fixed: before this wave a 500 synthesised `http_500` -> `widget.error.http_5xx` ("Something went wrong on our side…"), and shipping `code` without a matching key would have downgraded it to `widget.error.generic` ("Could not send…"); a `widget.error.internal` key restoring the 5xx wording was added at the critic's C-005 finding, pinned by a new `ui.test.ts` case, +86 B (`widget-bundle-size` PASS, 30,010/30,720, 710 B headroom). NOTE for anyone re-deriving this: C-005 cited `handlers/attachments.rs:213,247,461` as the reachable path and that specific attribution is WRONG — `widget.ts:289` swallows an attachment-upload failure (`catch { attachOk = false }`) and renders `widget.status.sentAttachmentsFailed`, so those three 500s never reach the error-key map at all. The conclusion held for a different reason: any database failure on the SUBMIT call becomes `ApiError::Internal` via `From<RepoError>` / `From<sqlx::Error>` / `From<anyhow::Error>` and lands in `widget.ts:301`'s catch -> `showError(apiErr.code)`. Residual: the three unkeyed codes stay unkeyed deliberately (no reachable path, and the widget budget is the scarce resource) | re-derived from code 2026-09-07 at the critic's C-005: every `ApiError::{Forbidden,Conflict,Gone,Internal}` construction site enumerated, the widget's route set and its two error-render paths read, `claim_idempotency_key` traced; oracle numbers measured before (796 B) and after (710 B) the added key | CLAUDE-E-collab-20260907-034037 (ledger not brief under the 2026-08-06 filing gate: the one finding with observed harm was fixed in the same wave; what remains is three codes with no reachable path. Promote if a widget-reachable handler is ever given `Forbidden`, `Conflict` or `Gone`)
2026-09-07 | `feedbackmonk_i18n::catalogs::interpolate` (`catalogs.rs:193-205`) substitutes its args SEQUENTIALLY with `String::replace` over the accumulating output, so a value that itself contains `{{later_arg}}` is re-substituted on a later iteration -- e.g. `interpolate("{{a}}", &[("a", "{{b}}"), ("b", "boom")])` yields `boom`, not the literal `{{b}}`. Order-dependent and therefore not a stable contract either. NO OBSERVED HARM: every current call site passes team- or system-authored values (`format!`-built counts, project names, status labels) and no submitter-controlled string is ever an interpolate arg -- the submitter's body reaches an email through `format!`, never through the catalog. Fix when it matters is a single-pass replace (scan the template once, look each `{{name}}` up in the arg list) rather than N passes | R-SEC (frontier, read-only) raised it as a §5.7 note in MSG-008 of PODS collab-20260907-034037; LEAD ruled "write it down rather than fix it" and routed the line to CLAUDE-F, who confirmed the mechanism by reading the implementation (not by executing it) | ledger not brief under the 2026-08-06 filing gate: internally discovered, no observed harm, and no call site can reach it today. PROMOTE THE MOMENT a user-controlled value becomes an `interpolate` arg -- i.e. any new `t_args`/`t_plural` call whose argument is submitter-authored text -- at which point it is a template-injection defect, not a note
2026-09-07 | The fa/tr plural-partition fix landed earlier today is UNPROVABLE by any in-tree oracle right now, and the ledger line claiming it is defended by `i18n-catalog-integrity` Probe D overstates what that probe can currently see. MEASURED A/B: with `fa`/`tr` back in `OTHER_ONLY` (the bug) the oracle prints `PASS i18n-catalog-integrity` / `Probe D (CLDR plural categories): PASS`; with them removed (the fix) it prints exactly the same, while `plural_categories("fa")` differs (`('other',)` vs `('one','other')`) -- i.e. Probe D's verdict is INVARIANT across the defect it nominally covers. Mechanism, and it is not a probe bug: `validate.py`'s `plural_category_gap` class skips any base whose locale catalog has NO category present (`if not present_cats: continue` -- an entirely untranslated base is not that class's concern), and all 150 non-`en` catalog files currently hold **0 translated leaves** (measured by flattening every one: `_meta`-only skeletons), so the class never fires for any locale, let alone for fa/tr. WHAT PROBE D CAN DISTINGUISH: a PARTIALLY translated base missing a category its locale requires -- demonstrated for exactly this fix in a temp mirror of `i18n/` (a Persian `public.roadmap.voteCount_other` with no `_one` is an ERROR under the fix and was silently accepted before). WHAT IT CANNOT: which of the two partitions is correct while the catalogs are empty -- the correctness evidence for this fix is the three plural-partition suites (red -> green -> inverse-red) and that temp-mirror run, NOT the oracle. EVIDENCE LOCATION IS THE HONEST GAP: the temp-mirror demonstration lives in a work-log section and in this ledger line, not in a `docs/falsifiability/` receipt like the one `widget-bundle-size` Probe C got (`docs/falsifiability/2026-09-07-widget-bundle-size-probe-c.md`), so the asymmetry is deliberate to record rather than to quietly tolerate. NO OBSERVED HARM: the fix is correct and independently ratcheted; what is missing is oracle-level falsifiability, which arrives on its own the moment `/1-translate` writes the first plural key into any `fa`/`tr` catalog -- from that instant Probe D distinguishes the two partitions with no code change | measured 2026-09-07 by CLAUDE-F of PODS collab-20260907-034037 at the LEAD's direction, from critic finding C-004; A/B run by toggling `OTHER_ONLY` in `scripts/i18n/_catalog.py` and re-running the oracle both ways (file restored, `git diff` verified, plural suites green after) | ledger not brief under the 2026-08-06 filing gate: this is a coverage gap in verification machinery with NO witnessed wrong verdict -- the probe has never passed something it should have failed, it simply cannot see this class yet. Deliberately NOT building a jig or a synthetic-catalog fixture for it (LEAD ruling): the gap closes itself at the next translation pass. PROMOTE only if `/1-translate` runs and Probe D still cannot distinguish the partitions afterwards, which would mean the class is genuinely unreachable rather than merely dormant
2026-09-07 | A CSI registry-write RACE was recorded during this wave's spawn burst: `.claude/collaboration/_registration-failures.jsonl` holds exactly one line -- `{"ts":"2026-09-07T04:29:03Z","kind":"registration-failed","id":"CLAUDE-D","reason":"entry-erased-by-third-party","pid":82060,"writer":"powershell"}` -- i.e. CLAUDE-D wrote its registry entry into `active-sessions.json` and a concurrent whole-file write by another process erased it between the write and the read-back verify. This is the exact failure class the CSI registry-write helper exists to prevent (one writer per file, read-back verification), which is also why it was DETECTED rather than silent: the write verified itself, found its own entry gone, and left a durable record instead of a session that quietly is not dispatchable. NO OBSERVED HARM: CLAUDE-D ran the entire wave, was addressable by the LD, took a reopening at 10:09Z and completed; nothing downstream depended on the erased entry surviving. Recorded so a recurrence has a FIRST DATA POINT with a timestamp, a PID and a writer (`powershell`) to correlate against -- one occurrence in a five-session burst is unremarkable, a second would not be. A REPEAT would mean a writer bypassing the helper (the helper cannot be raced by two of its own callers), so the diagnostic question on recurrence is "which process wrote `active-sessions.json` without going through it", not "is the helper's locking wrong" | found 2026-09-07 by CLAUDE-F of PODS collab-20260907-034037 while closing critic finding C-002 (the file surfaced as the last untracked, unignored entry under `.claude/collaboration/`; now covered by `.gitignore`'s `**/.claude/collaboration/_*.jsonl`), read in full and reported to the LEAD, who directed this line | ledger not brief under the 2026-08-06 filing gate: internally discovered, no observed harm, and the mechanism behaved as designed -- a detection that fired, not a defect that escaped. No fix and no brief by LEAD ruling. PROMOTE on a second occurrence, or on any occurrence whose erased entry left a session undispatchable
2026-09-07 | The admin-ui vitest suite now flakes on a COLD run: the first full `npm run test` after this wave's changes failed 3 tests in `src/pages/autopilot/__tests__/NewStory.test.tsx` on vitest's default 5000 ms per-test timeout; the same three pass in isolation in ~0.8-1.1 s each, and a warm re-run of the full suite passed 219/219 in half the wall-clock (33.6 s -> 16.7 s). Cause is cold-transform contention, not a behaviour change: the Stage-2 extraction routes every admin component render through the i18next bootstrap, so first-run transform+collect roughly doubles and the slowest tests cross the default limit. NO OBSERVED HARM AND IT CANNOT TURN CI RED: `.github/workflows/ci.yml` runs only `verification-oracles`, `cargo build/test/clippy` and `cargo-deny` -- it does not run vitest or Playwright at all (checked, not assumed). The cost lands on a developer running a cold `npm run test` and reading a red suite that is not red | measured at PODS collab-20260907-034037 convergence Phase 5 by the LEAD: run 1 of `npm run test` -> `3 failed | 216 passed`, all three `Test timed out in 5000ms`; `npx vitest run src/pages/autopilot/__tests__/NewStory.test.tsx` -> 7/7 pass; run 2 of the full suite -> `28 passed (28) / 219 passed (219)`; `grep -n "run:" .github/workflows/ci.yml` shows no npm/vitest/playwright step | LEAD-collab-20260907-034037 (ledger not brief under the 2026-08-06 filing gate: internally discovered, unreproduced on the warm path, and invisible to CI. The obvious remedy -- raising `testTimeout` in the vitest config -- was deliberately NOT taken at a convergence, where a config change that makes a red suite green is exactly the shape that should not be made under time pressure. PROMOTE if it recurs on a warm run, or if the suite is ever added to CI, which would make it a real gate flake)
2026-09-07 | The admin/public SPA has NO React error boundary anywhere, so any render-time throw unmounts the whole tree and paints an empty document -- no error UI, no `role="alert"`, nothing for a screen reader to land on. Two independent measurements this session: R-SEC demonstrated `?lang=constructor` reaching `formatRelative` and throwing `RangeError: Incorrect locale information provided` on a public board (that specific trigger is now fixed at the locale gate); R-A11Y independently reproduced a whole-page blank on `/admin/settings/board` from a deliberately wrong-shaped mock response (`TypeError: Cannot read properties of undefined (reading 'length')` escaping to `pageerror`). The realistic trigger is server-shape drift, and THIS PROJECT HAS ALREADY SHIPPED THAT EXACT FAILURE ONCE: the `wont-fix` serde mismatch that white-screened the live admin at `triage.gitcellar.com` (CLAUDE.md § PF-PHASEA-01), which was hardened per-component (`?? []` + label fallbacks) rather than structurally. NO OBSERVED HARM FROM THIS WAVE -- both triggers were induced by the reviewers, and the locale one is closed | measured at PODS collab-20260907-034037: R-SEC MSG-008 (jsdom + the built dist glob map) and R-A11Y MSG-010 (Playwright over the built `admin-ui/dist`, `pageerror` captured); `grep -rn "componentDidCatch\|ErrorBoundary\|errorElement" admin-ui/src` -> no matches | LEAD-collab-20260907-034037 (ledger not brief under the 2026-08-06 filing gate: no unprompted user-facing occurrence in this wave, and an error boundary is an architecture decision -- where to place it, what it renders, whether routes get their own -- not a one-line fix to make at a convergence. PROMOTE on the next witnessed white-screen from shape drift, or fold it into whichever session next touches `App.tsx`'s routing shell)
2026-09-07 | Stray `.claude/session-state/` directories appear inside the source tree -- `admin-ui/`, `admin-ui/src/`, `admin-ui/src/i18n/` and `admin-ui/src/pages/autopilot/` -- 15 files in total, written when a worker `cd`s into a subdirectory and the framework hooks resolve the project root from the process cwd rather than from the git root. NO HARM AND NOTHING COMMITTABLE: all 15 are matched by `.gitignore:50 **/.claude/session-state/` (verified per-file with `git check-ignore`, not inferred from the pattern). It is cosmetic pollution of a source tree plus a small trap for anyone grepping `admin-ui/src` recursively | measured at PODS collab-20260907-034037 by the LEAD while checking whether a worker's subdirectory writes could reach the commit: `find admin-ui/src -name .claude -type d` -> 3 hits (plus `admin-ui/.claude`), then every file under them passed to `git check-ignore -q` -> all ignored | LEAD-collab-20260907-034037 (ledger not brief under the 2026-08-06 filing gate: internally discovered, no observed harm, and the fix belongs upstream in the ULDF hooks' project-root resolution rather than in this repo. Deliberately NOT cleaned up mid-wave -- deleting them while five workers were live would simply have regenerated them. Filed to ULDF with this wave's other injects. PROMOTE if one ever lands outside `.claude/session-state/` and so escapes the ignore pattern)
2026-09-07 | Four directories under `admin-ui/src` are modules by the ULADP standard but carry no `README.md`: `src/pages` (fixed this wave -- CLAUDE-D authored it, since it rewrote nearly every file in the directory), `src/shared`, `src/pages/roadmap` and `src/pages/public`. The `module-index` oracle does not surface them as gaps because it enumerates directories that already have a README and reports `has_readme` for those -- so an absent module README is invisible to it by construction, which is the harder half of this finding: the oracle answers "are the modules I can see documented?" and not "which directories are modules?" | measured at PODS collab-20260907-034037: `bash .claude/oracles/module-index/run.sh` -> 87 modules, `no README: []`, and `admin-ui/src/pages` / `src/shared` / `pages/roadmap` / `pages/public` absent from the roster entirely, cross-checked against `find admin-ui/src -name README.md` -> 6 hits | LEAD-collab-20260907-034037 (ledger not brief under the 2026-08-06 filing gate: pre-existing debt unrelated to FR-FBR-38, and deliberately not added to a cheap-tier lane on the critical path. The oracle-blindness half is the part worth promoting -- PROMOTE if a module-README coverage check is ever wanted as a gate, since `module-index` cannot currently provide one)
