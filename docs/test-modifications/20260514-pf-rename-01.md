# PF-RENAME-01 — Test-file modifications justification

**Date**: 2026-05-14
**Scope**: All test files under `crates/feedbackmonk-*/tests/` (formerly `crates/feedbackr-*/tests/`)
**Commit**: `chore: rename feedbackr-* → feedbackmonk-* (PF-RENAME-01)`

## What changed in tests

- File paths: `crates/feedbackr-*/tests/*.rs` → `crates/feedbackmonk-*/tests/*.rs` (git mv only)
- Inside each test file: `use feedbackr_*::…` → `use feedbackmonk_*::…` (identifier rename only)
- HTTP header literals: `X-Feedbackr-Anon-Cookie` → `X-Feedbackmonk-Anon-Cookie`
- Cookie name literals: `feedbackr_session` → `feedbackmonk_session`
- Other brand literals where present in test assertions (e.g. `feedbackr.local` → `feedbackmonk.local`).

## Why this is not reward-hacking

- **No assertions changed in meaning.** Every assertion checks the same property; only the literal token being checked was substituted because the underlying production code now emits the substituted token. The pre-rename and post-rename test would be byte-identical if you also substituted in the production code, which is exactly what this commit does.
- **No tests added, removed, or disabled.** `cargo test --workspace` ran post-rename and produced the same count (218 passed) as the P1 Stage 3 closing commit at 835fbf8.
- **No expected values relaxed.** The 20-pattern PII scrubber `expected_hash.txt` is unchanged — the pattern set is identical, so the hash oracle (Probe B) still matches.
- **Both Verification Oracles GREEN** after rename: `multi-tenant-isolation-check` PASS, `pii-scrub-audit` PASS.

## Trigger

DEC-FBR-11 (working-name change Feedbackr → feedbackmonk) + PF-RENAME-01 in project CLAUDE.md.

## Anti-Reward-Hacking Gate disposition

This is a mechanical identifier-substitution rename, not a behavioral change. The substitution is bounded: the same string tokens that appear in production code now also appear in tests; no test was modified to accommodate a behavior change in the system under test.
