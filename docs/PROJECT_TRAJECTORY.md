# Project Trajectory — feedbackmonk

Rolling high-level state. Auto-maintained by `/0-uldf-finalize` Phase 12. Cheap orientation for fresh sessions; for detail, go to `docs/specs/` and `docs/planning/`.

**Last updated**: 2026-05-14 (P4 Stage 1 CLOSED — brand pass + interface-contract freeze (BRAND.md C20 + SELFHOST_ENV.md C21 + marketing/ + deploy/docker/ skeletons + DEC-FBR-IMPL-05 pricing-SSOT + DEC-FBR-IMPL-06 selfhost-compose-smoke oracle decision); all 4 existing Verification Oracles GREEN; FR-FBR-16/17 remain PROPOSED — Stage 3 convergence flips them; Stage 2 ahead — two-worker PODS fan-out)

> **Working name**: "Feedbackr" through P0/P1; renamed to **feedbackmonk** on 2026-05-14 per DEC-FBR-11. Identifier prefixes `FR-FBR-*` / `DEC-FBR-*` are stable per the ID-stability rule. Code-level `feedbackr-*` → `feedbackmonk-*` rename committed in PF-RENAME-01 (commit `82a2e59`). Pending user actions: PF-RENAME-02 (working-dir rename) + PF-REGISTER-01 (org+domain registration).

---

## Current Focus

**P4 Stage 2 ahead — two-worker PODS fan-out (marketing site + self-host docker)**. Stage 1 froze the three contracts (BRAND.md C20, SELFHOST_ENV.md C21, pricing-SSOT mechanism via DEC-FBR-IMPL-05) and decided the selfhost-compose-smoke oracle build-at-Task-Zero (DEC-FBR-IMPL-06). Next: `/0-uldf-pods-parallelize` per the P4 plan; CLAUDE-A consumes BRAND.md + Astro init + Rust→JSON pricing pipeline (Worker A Task Zero); CLAUDE-B consumes SELFHOST_ENV.md + builds the `selfhost-compose-smoke` oracle (Worker B Task Zero) before docker-compose authoring.

P4 Stage 1 (this commit) is pure documentation + scaffold work — zero code touched. The arc continues; FR-FBR-16 (marketing) + FR-FBR-17 (self-host) stay PROPOSED until Stage 3 convergence. P3 closure preserved below for historical context.

---

**P3 CLOSED — admin UI tier settings ships on top of Stage 1's backend in one commit.**

P3 Stage 2 (S002 orchestrator → Stage 2 worker, single-worker HANDOFF topology consuming Stage 1's frozen handoff brief verbatim) delivers the user-facing surface for FR-FBR-14: (a) `/admin/settings/tier` "Plan & usage" page (current-tier card + per-resource UsageMeter + capability matrix + UpgradePrompt CTA); (b) `UsageMeter` with WCAG 1.4.1 dual-encoding (color token + explicit "OK" / "Approaching cap" / "Over cap" text in `aria-valuetext`) and `unlimited` rendering convention (no progressbar when `limit === null`); (c) `UpgradePrompt` rendering nothing on Self-host and a mailto stub on Free/Starter/Pro per DEC-FBR-DEFER-01 (no checkout flow); (d) `ApiClient.fetchTierStatus()` (Contract C17 reader) + 402/409 axios interceptor that tags `err.tierCapExceeded` with the parsed Contract-C18 body + `extractTierCapExceeded(err)` helper for future mutation `onError` callers; (e) Stage-2-side drift-surface — `TierSettings.test.tsx`'s `tierStatus()` helper inlines the four-tier Contract C19 quotas verbatim, FAILS on Stage 1 rebase (paired with `tier-enforcement-status` Probe B). Polar billing remains **deferred** per DEC-FBR-DEFER-01 — Upgrade button is the explicit "Contact support to upgrade" mailto stub.

P3 Stage 1 (commit `d2266ae`, preserved for historical context) delivered the backend commercial-gate: `Tier` enum + `tier_quotas()` const fn (Contract C19); `TierQuotaRepo::check_tier_quota` chokepoint wired pre-INSERT in projects + feedback handlers (Contract C17); `ApiError::TierCapExceeded` 402/409 mapping (Contract C18); tier-aware `TenantRepo::get_widget_brand` enforcing FR-FBR-14 free-tier footer; `GET /api/v1/admin/tier` admin status endpoint; `tier-enforcement-status` Verification Oracle (Probe A AST + Probe B Contract-C19 shape + Probe C `--full` integration smoke trio); `migrations/00008_tenant_tier_check.sql` defense-in-depth; `docs/operations/TIER_OVERRIDE.md` operator helper; `docs/deferred/polar-integration.md` deferred-stub for FR-FBR-15.

- **P3 exit gate**: PASSED — vitest 38/38 (was 25/25 pre-Stage-2; +13 new in `TierSettings.test.tsx`), Playwright `tier-settings-a11y` 4/4 (0 axe-core violations on Free/Starter/Pro/Self-host), all 4 Verification Oracles GREEN at finalize (`multi-tenant-isolation-check`, `pii-scrub-audit`, `widget-bundle-size`, `tier-enforcement-status` active-PASS), vite build 263.50 kB (84.02 kB gzipped), FR-FBR-14 fully DONE in `docs/specs/SPECIFICATION.md`, FR-FBR-15 DEFERRED.

P2 convergence (commit `9f1a28b`) delivered the customer-facing surface (widget + public roadmap + Q24 promote) — preserved here as historical context.

## Active Threads

- **P4 Stage 1 CLOSED — brand pass + interface-contract freeze** (this commit): pure docs/scaffold commit. Six files: P4 plan (`docs/planning/plans/20260514T163356-feedbackmonk-p4-go-public.md`), brand kit (`docs/brand/BRAND.md` = Contract C20: ink/cream/sage palette + self-hosted Inter/JetBrainsMono + wordmark-only v1 + monastic-craft voice), env-var catalog (`docs/operations/SELFHOST_ENV.md` = Contract C21), two top-level skeleton READMEs (`marketing/README.md` + `deploy/docker/README.md` — ULADP six-section, link to canonical contracts), DECISIONS.md additions (DEC-FBR-IMPL-05 + DEC-FBR-IMPL-06). HERE topology chosen over HANDOFF (light consumption ~15-25%; plan written in this session, no cold-start need). All 4 existing Verification Oracles trivially GREEN — Stage 1 didn't touch their trigger paths.
- **P3 CLOSED — admin UI tier settings + cap-aware error rendering** (commit `df07241`): S002 orchestrator HANDOFF to Stage 2 worker; worker delivers `pages/settings/` module (TierSettings + UsageMeter + UpgradePrompt) + `ApiClient` extensions (`fetchTierStatus` + 402/409 interceptor + `extractTierCapExceeded`) + Stage-2-side drift fixture. Arc-terminus per CSI-06 — P3 closes; P4 begins next.
- **P3 Stage 1 CLOSED — backend commercial-gate** (commit `d2266ae`): tier model + cap-firing predicate + admin status endpoint + Verification Oracle + deferred-Polar stub.
- **P2 CLOSED — convergence commit** (commit `9f1a28b`): three-worker PODS fan-out converges. CLAUDE-A widget + Task Zero oracle + Contract C12; CLAUDE-B migrations 00006/00007 + repo layer + voting cache + 8 HTTP endpoints (C13/C14/C15); CLAUDE-C promote handler with Q24 byte-for-byte ports + admin-ui Roadmap pages (C16). All 4 FR-FBR P2 requirements flipped DONE.
- **PF-RENAME-01 — DONE** (commit `82a2e59`): code-level brand rename `feedbackr-*` → `feedbackmonk-*` at the P1→P2 quiescent boundary.
- **P1 CLOSED — arc-terminus** (commit `835fbf8`): Stage 3 e2e witness + critic C-002 + 4 module READMEs. 218 workspace tests; FR-FBR-07/08/09/10 all DONE.
- **P1 Stage 2 — DONE** (commit `d6f247a`): admin transition + reply HTTP (C7), admin list + detail HTTP (C8), `feedback_replies` migration 00004, three plain-text email templates (C10), Mailpit integration test, `admin-ui/` React+Vite directory + Playwright+axe a11y smoke.
- **P0 Foundation — COMPLETE** (commit `b9a672a`): all 5 P0 FRs DONE; e2e P0 PASS 7/7.
- **Pending pre-public-push gates (USER ACTION)**:
  - **PF-REGISTER-01** — Register `github.com/feedbackmonk` org + buy `feedbackmonk.com` (~$10/yr). Confirmed AVAILABLE 2026-05-14; not yet claimed. Push remains GATED until this clears.
  - **AGPL LICENSE**: full AGPL-3.0 text in `LICENSE` (replaced 2026-05-13) — gate CLEARED on the LICENSE side.
- **Pending follow-ups**:
  - **PF-RENAME-02 (USER ACTION, pending)** — Working-directory rename `Apps\Feedbackr` → `Apps\feedbackmonk`. Requires user action (Windows cannot rename CWD of live process). Surfaced at this quiescence boundary.
  - **DEC-PODS-LEAD-01 framework improvement** — `monitor-pods.ps1` regex should accept `CONVERGENCE-READY` alongside `COMPLETED` as a terminal status label (TIMEOUT artifact observed at convergence; not a real blocker).
  - **rollup-win32-x64-msvc npm bug 4828 workaround** — when widget CI lands, automate the manual binary-extraction fallback.
- **LTADS S001** — CONCLUDED at P1 close (commit `835fbf8`). P2 convergence ran outside LTADS (LTADS-not-active path through `/0-uldf-pods-converge --finalize`).
- **GitCellar widget-embed touchpoint — DEFERRED to late P2 / early P3**: first cross-repo integration when GitCellar embeds the feedbackmonk widget as customer #1.

## Recent Decisions

- **DEC-FBR-IMPL-05** (P4 Stage 1) — Pricing single-source-of-truth = build-time Rust→JSON export from `feedbackmonk-core::tier::tier_quotas()` (Contract C19), NOT hand-typed pricing constants in Astro/MDX. Worker A's Task Zero implements the binary + shim + Astro `prebuild` wiring. Makes site↔code drift structurally impossible (transitively verified via `tier-enforcement-status` Probe B). Withdraws the `marketing-pricing-parity` oracle candidate as redundant under Option A.
- **DEC-FBR-IMPL-06** (P4 Stage 1) — Build new `selfhost-compose-smoke` Verification Oracle as Worker B Task Zero. Three probes: A (yaml-lint), B (env-var doc cross-reference vs SELFHOST_ENV.md catalog), C (`--full` clean-state docker smoke). Defends FR-FBR-17 exit gate (Testability-Gate composite ~14). Mirrors P3 Stage 1 Task Zero pattern for `tier-enforcement-status`.
- **Brand kit decisions** (P4 Stage 1, recorded in `docs/brand/BRAND.md` Decision Log) — (a) Palette = ink/cream/sage + variations for "monastic craft" semantic alignment with "monk" wordmark; (b) Self-hosted Inter + JetBrains Mono fonts under `marketing/public/fonts/` — extends DEC-FBR-02 no-third-party-trackers brand promise to font-loading (no Google Fonts CDN); (c) Wordmark-only v1, logo-mark deferred to post-launch designer engagement.
- **Top-level directory choices** (P4 Stage 1) — `marketing/` (vs `site/`/`landing/`/`www/`) chosen for precision; `deploy/docker/` (vs root `docker-compose.yml`, vs `docker/`) chosen to leave room for future `deploy/helm/` or `deploy/k8s/` siblings without root pollution. Both rationales in their READMEs.
- **D-FBR-22 / D-FBR-23 / D-FBR-24** (P3 Stage 2 — generalizable patterns) — Three patterns surfaced and recorded in `docs/specs/DISCOVERIES.md`: (a) WCAG 1.4.1 dual-encoding meter pattern (color token + explicit text status in `aria-valuetext` — meaning is decodable in monochrome, by screen readers, and by sighted users; reusable for any future quantitative widget); (b) "Tag the AxiosError, don't fire from the interceptor" — interceptor lives outside React context, so toast-via-context can't fire there; instead tag `err.tierCapExceeded` and provide an `extractTierCapExceeded(err)` seam for callers (avoids over-engineering when no current mutation paths consume it); (c) `unlimited` rendering convention — flat text, no progressbar, when the cap is null (a 0% or 100% bar would mislead). All three documented in DISCOVERIES.md.
- **WCAG-conformance work-shape** (P3 Stage 2) — Stage 2 worker chose `aria-valuetext` over `aria-valuenow + visible numeric label only` for the UsageMeter. Rationale: `aria-valuenow=42` reads as "42" alone; `aria-valuetext="12 of 50 projects used (24%, ok)"` is the canonical screen-reader payload that fully conveys state. Pattern documented in `admin-ui/src/pages/settings/README.md` Decision Log.
- **DEC-FBR-DEFER-01** (P3 Stage 1; held through Stage 2) — Polar billing (FR-FBR-15) **deferred** from P3 per user direction at plan boundary. P3 ships tier model + cap enforcement + free-tier footer + admin tier-status endpoint + admin UI surface per FR-FBR-14 but NOT a Polar webhook receiver / customer subscription schema / self-service upgrade flow. Stage 2 admin "Upgrade" button is a mailto stub (`mailto:support@feedbackmonk.com?subject=Upgrade%20request`). Operator promotion via SQL helper at `docs/operations/TIER_OVERRIDE.md` until Polar lands. Webhook envelope + event-mapping + schema migration shape captured in `docs/deferred/polar-integration.md` for future port from `gitcellar-cloud/src/billing/`.
- **D-FBR-19 / D-FBR-20 / D-FBR-21** (P3 Stage 1) — Three new generalizable patterns surfaced: schema-CHECK + Rust-codec defense-in-depth pairing for tier-shaped enumerated columns (catches structurally-different drift); eager Probe C smoke-test authoring upgrades vacuous-PASS oracle to active-PASS at marginal cost; test-mod artifact YAML `tests_modified[]` frontmatter as upfront enumeration + cross-check (active defense against D-FBR-17 missed-fixture-site failure mode). All three documented in `docs/specs/DISCOVERIES.md`.
- **Worker chose AND, not OR** (P3 Stage 1) — Plan offered "schema CHECK OR Rust strict parser." Worker chose both. Schema CHECK catches direct DB writes that bypass Rust; sqlx codec catches programmatic mistakes earlier in the loop. Documented in `migrations/00008_tenant_tier_check.sql` header + `feedbackmonk-core/README.md` Decision Log.
- **DEC-PODS-A-01 / A-02** (P2 convergence) — Widget tracker hostname list expanded 8 → 18 (additive only, sha256 `7823d6e6…`; defends DEC-FBR-02 brand promise against wider surface than seed list); widget e2e port 14206 deconflicted from CLAUDE-C's 14205 admin-ui e2e (DEC-FBR-IMPL-04 port-deconflict pattern). Both RATIFIED at convergence; port row propagated to `~/.claude/MACHINE_CONFIG.md` Dev Port Registry.
- **DEC-PODS-B-01 / B-02 / B-03** (P2 convergence) — Allowlist structural-mirror constructor entries for `SqlxRoadmapItemRepo::new` + `SqlxRoadmapVoteRepo::new`; AppState fixture extensions justified via `docs/test-modifications/20260514-p2-appstate-roadmap-fields.md` (extended at convergence to cover all 4 co-edits — 3 AppState extensions + 1 `TenantRepo::get_widget_brand` mock fill-in); cache + helper widenings (60s default retraction window, `LEFT JOIN` in `aggregate_vote_counts`, defense-in-depth `from_db_str` Anon fallback). All RATIFIED.
- **DEC-PODS-C-01 / C-02 / C-03 / C-04** (P2 convergence) — `RoadmapItem` TypeScript optional widenings (`origin_feedback_id?`, `voted_by_me?`); admin roadmap URL omits project segment (`/admin/roadmap`; sole-project resolution via `fetchAdminProjects`); promote module README inlined as Rust `//!` module-doc (single-file module convention); 4 net-new slug-helper tests beyond gitcellar's 6 Q24 byte-for-byte ports (orthogonal to Q24 — slug helper is feedbackmonk-only). All RATIFIED.
- **DEC-PODS-LEAD-01** (P2 convergence) — Convergence proceeds despite `monitor-pods.ps1` TIMEOUT (regex artifact, not real blocker — monitor matched only `COMPLETED` and missed CLAUDE-A's `CONVERGENCE-READY` label). All exit-gate items GREEN per worker reports. Framework-level improvement candidate documented as follow-up.
- **PF-RENAME-01 executed** (commit `82a2e59`) — Mechanical brand-rename at the P1→P2 quiescent boundary: kebab `feedbackr-*` → `feedbackmonk-*`, env prefix `FEEDBACKR_` → `FEEDBACKMONK_`, header `X-Feedbackr-Anon-Cookie` → `X-Feedbackmonk-Anon-Cookie`, session cookie `feedbackr_session` → `feedbackmonk_session`, BLAKE3 domain separator `b"feedbackr-anon-v1"` → `b"feedbackmonk-anon-v1"`. ID prefixes `DEC-FBR-*` / `FR-FBR-*` preserved per DEC-FBR-11. Crate renames via `git mv` (history preserved).

## Risks

| Risk | Stage | Notes |
|---|---|---|
| **GitHub org + domain registration** | Pre-public | User-action pending (PF-REGISTER-01). Push remains GATED on this until cleared. Both `github.com/feedbackmonk` and `feedbackmonk.com` confirmed AVAILABLE 2026-05-14; `gh api orgs --method POST -f login=feedbackmonk` + `feedbackmonk.com` purchase (~$10/yr). |
| **Q24 byte-for-byte invariant drift** | P2 → forever | **MITIGATED** — `promote.rs::tests` module has `#[allow(clippy::uninlined_format_args, clippy::doc_markdown)]` to preserve byte-for-byte invariant against future lint drift; ULADP module-doc explicitly marks render functions + 6 ported tests as UNTOUCHABLE. Drift is unlikely without deliberate edit. |
| **In-memory anonymous rate-limiter loses state on restart** | P0 (deferred to v1.1) | Acceptable for single-instance dogfood; non-breaking Redis backend swap planned for v1.1. See DISCOVERIES.md D-FBR-08. |
| **Voting cache cold-start cost** | P2 → P3 | First request for a project after restart triggers lazy warming; 60s thereafter cached. If P3 launches show cold-start latency issues, consider eager warming for top-N projects at boot. |
| **Widget node_modules rollup npm bug 4828** | P2 dev / CI | **MITIGATED locally** — manual binary extraction via `npm pack` documented in CONVERGENCE_REPORT. CI will need automation when widget e2e CI lands. |
| **PII scrub drift** | P1+ | **MITIGATED** — `pii-scrub-audit` oracle GREEN at every convergence. |
| **Multi-tenant isolation drift** | P0+ | **MITIGATED** — `multi-tenant-isolation-check` oracle GREEN; allowlist additions are structural-mirror constructor entries only. |
| **`tenant_users` table doesn't exist yet** | P1/P2 carry-forward | Migration 00004's `author_user_id` is bare UUID with no FK. Add FK with `ALTER TABLE … ADD CONSTRAINT … NOT VALID; VALIDATE CONSTRAINT …` when `tenant_users` lands (P3 multi-admin work, FR-FBR-15). |
| **GitCellar peer repo coordination** | Late P2 / early P3 | First cross-repo touchpoint imminent when GitCellar embeds the feedbackmonk widget as customer #1. Forward-looking only; not a P2 blocker. |

## Next-Best-Steps

1. **Spawn P4 Stage 2 PODS** — `/0-uldf-pods-parallelize` per the P4 plan (`docs/planning/plans/20260514T163356-feedbackmonk-p4-go-public.md`). Two parallel workers: CLAUDE-A (marketing site — Worker A Task Zero is the Rust→JSON pricing pipeline per DEC-FBR-IMPL-05, then Astro init + page authoring); CLAUDE-B (self-host docker — Worker B Task Zero is the `selfhost-compose-smoke` oracle per DEC-FBR-IMPL-06, then docker-compose.yml + Dockerfile.api + migrate.sh + backup.sh + `.env.example`). Both consume the frozen contracts: BRAND.md (C20), SELFHOST_ENV.md (C21), DEC-FBR-IMPL-05 (pricing-SSOT mechanism).
2. **P4 Stage 3 convergence** (after both workers ship) — flip FR-FBR-16 + FR-FBR-17 to DONE in SPECIFICATION.md; add `marketing/` + `deploy/docker/` modules to ARCHITECTURE.md (with actual content); run all 5 Verification Oracles (4 existing + new `selfhost-compose-smoke`) GREEN; ratify any worker decisions.
3. **PF-REGISTER-01 (USER ACTION)** — Register `github.com/feedbackmonk` org and purchase `feedbackmonk.com` before any first public push. Both confirmed AVAILABLE 2026-05-14; `gh api orgs --method POST -f login=feedbackmonk` or web UI. Push remains GATED on this until cleared.
4. **PF-RENAME-02 (USER ACTION)** — Working-directory rename `Apps\Feedbackr` → `Apps\feedbackmonk`. Must run with no live Claude Code sessions in the directory. PowerShell: `Rename-Item "E:\Developer\SourceControlled\Apps\Feedbackr" "feedbackmonk"`. Update `~/.claude/MACHINE_CONFIG.md` Dev Port Registry row afterward.
5. **Widget CI hardening** — automate the rollup-win32-x64-msvc fallback when widget e2e CI lands (npm bug 4828).
